## Software Design Principles

By the help of these principles we can make our code more
--> Readable
--> Structure
--> Maintainable
--> Extendible
--> Code Quality

In this we don't care about Algorithm, we care about problem in code Quality

## In the Example 1 of Frontend -->
The function UserProfile is doing too many things. And it voilates the Single Responsibility Principle
Single means that some works doing in isolation.
Responsibility means the work we can do

--> Issue called --> Maintainence and Extendibility

How to detect if SRP is not fulfilled??
--> Monster function --> called Big Function (Example -> In Node.js/Express.js We have controller 
if there is lot of request, response, db interaction then these function are called Monster Function).
--> Common/Utils folder
--> Too much if else